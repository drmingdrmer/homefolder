#!/bin/sh

_author=$(git config --local --get user.name)
_days=${1-5}
git shortlog \
    --author="$_author" \
    --since="$_days days ago" \
    --no-merges \
    --format="%ad %s" \
    | awk '{$4="";$5="";$6=""; print $0}'
