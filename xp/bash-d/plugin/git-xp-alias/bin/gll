#!/bin/sh

flags=$1
[ ".$flags" != "." ] && shift

eval git log --color \
    --decorate \
    --abbrev-commit \
    --find-renames \
    "$(_git_xp_interrept_flag lG$flags)" "$@" \

    # | sed 's/|/|                         /' \
    # | less -R --quit-if-one-screen -X
