#!/bin/sh

flags=$1
[ ".$flags" != "." ] && shift

export GIT_COMMA_FLAG=glo

# eval git \
eval git \
    $(_git_xp_interrept_flag conf $flags) \
    log --color \
    --decorate \
    --abbrev-commit \
    --find-renames \
    "$(_git_xp_interrept_flag arg $flags)" "$@"
