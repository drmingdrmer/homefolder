#!/bin/sh

local_commit=
if git rev-parse --quiet --verify $1 >/dev/null; then
    local_commit=$1
    shift
else
    local_commit=HEAD
fi

remote_head=$1
shift

git push origin $local_commit:refs/heads/feature/$remote_head "$@"
