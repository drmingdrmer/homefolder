if !has("unix")
    finish
endif

if has("nvim")
    finish
endif

" nvim use 'shada' instead of 'viminfo' and nvim can not read viminfo format
set viminfo+=n~/tmp/_viminfo

" vim: set ts=72 sw=4 sts=4 list :
